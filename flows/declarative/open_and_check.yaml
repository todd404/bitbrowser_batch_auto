name: "open_and_check"
version: 1
inputs:
  url:
    type: string
    required: true
steps:
  - action: goto
    url: "{{ inputs.url }}"
  - action: wait_for_url
    url: "**"
    timeout_ms: 60000
  - action: screenshot
    name: "final"
    full_page: true

